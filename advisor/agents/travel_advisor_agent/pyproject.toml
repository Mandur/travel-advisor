[project]
name = "travel-advisor-agent"
version = "0.1.0"
description = "Travel advisor agent that surfaces hotel pricing and forecasting information"
requires-python = ">=3.12"
dependencies = [
    "shared",
    "azure-identity>=1.15",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
shared = { workspace = true }
