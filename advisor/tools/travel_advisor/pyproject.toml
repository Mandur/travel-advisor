[project]
name = "travel-advisor"
version = "0.1.0"
description = "Travel advisor tools — hotel pricing and forecasting"
requires-python = ">=3.12"
dependencies = [
    "shared",
    "azure-identity>=1.15",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
shared = { workspace = true }
